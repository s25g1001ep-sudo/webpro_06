<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>人工衛星管理システム</title>
  <style>
    /* 人工衛星システム専用：テックブルー・テーマ */
    body { background-color: #0b1424; color: #e0e0e0; font-family: 'Segoe UI', sans-serif; padding: 20px; }
    h2 { color: #81d4fa; border-bottom: 3px solid #0277bd; padding-bottom: 10px; }
    table { width: 100%; border-collapse: collapse; background: rgba(0, 105, 192, 0.1); border: 1px solid #01579b; }
    th { background-color: #01579b; color: white; padding: 15px; text-align: left; }
    td { border-bottom: 1px solid #1a237e; padding: 12px; }
    tr:hover { background-color: rgba(129, 212, 250, 0.1); }
    a { color: #4fc3f7; text-decoration: none; font-weight: bold; }
    .btn { display: inline-block; margin-top: 20px; padding: 12px 24px; background-color: #00796b; color: white; border-radius: 4px; text-decoration: none; }
  </style>
</head>
<body>
  <h2>JAXA 稼働中人工衛星データベース</h2>
  <table>
    <tr><th>ID</th><th>人工衛星名</th><th>打ち上げ日時</th><th>打ち上げ場所</th></tr>
    <% let number = 0 %>
    <% for( let row of data ) { %>
      <tr>
        <td><%= row.id %></td>
        <td><a href="/satellite/<%= number %>"><%= row.name %></a></td>
        <td><%= row.date %></td>
        <td><%= row.location %></td>
      </tr>
      <% number += 1 %>
    <% } %>
  </table>
  <a href="/satellite/create" class="btn">＋ 新規衛星を追加</a>
</body>
</html>