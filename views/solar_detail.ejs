<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= data.name %>の詳細</title>
  <style>
    body { background-color: #0b0d17; color: #e0e0e0; font-family: sans-serif; padding: 20px; }
    h2 { color: #4fc3f7; }
    table { width: 600px; border-collapse: collapse; margin-bottom: 20px; }
    th, td { border: 1px solid #333; padding: 12px; text-align: left; }
    th { background-color: #1a237e; width: 150px; }
    .nav-links a { margin-right: 15px; color: #4fc3f7; text-decoration: none; }
    .delete-btn { color: #ff5252 !important; }
  </style>
</head>
<body>
  <h2><%= data.name %>の詳細情報</h2>
  <table>
    <tr><th>ID</th><td><%= data.id %></td></tr>
    <tr><th>天体名</th><td><%= data.name %></td></tr>
    <tr><th>区分</th><td><%= data.type %></td></tr>
    <tr><th>直径 (km)</th><td><%= data.diameter %></td></tr>
    <tr><th>備考</th><td><%= data.note %></td></tr>
  </table>
  <div class="nav-links">
    <a href="/solar/edit/<%= id %>">編集</a>
    <a href="/solar/delete/<%= id %>" class="delete-btn" onclick="return confirm('本当に削除しますか？')">削除</a>
    <a href="/solar">一覧に戻る</a>
  </div>
</body>
</html>