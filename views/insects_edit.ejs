<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= data.name %>の編集</title>
  <style>
    /* 虫システム専用：ネイチャーグリーン・テーマ */
    body { background-color: #1b2e1b; color: #f1f8e9; font-family: sans-serif; padding: 20px; }
    h2 { color: #c5e1a5; border-bottom: 2px solid #558b2f; padding-bottom: 10px; }
    
    table { width: 100%; border-collapse: collapse; margin-top: 20px; background: rgba(255, 255, 255, 0.05); }
    th { background-color: #33691e; color: white; width: 150px; padding: 12px; text-align: left; border: 1px solid #444; }
    td { padding: 10px; border: 1px solid #444; }
    
    input[type="text"] { 
      width: 95%; 
      padding: 8px; 
      background: #2e3d2e; 
      color: white; 
      border: 1px solid #558b2f; 
      border-radius: 4px;
    }
    
    .btn-group { margin-top: 20px; }
    input[type="submit"] { 
      padding: 10px 30px; 
      background: #689f38; 
      color: white; 
      border: none; 
      cursor: pointer; 
      border-radius: 4px; 
      font-size: 16px;
    }
    input[type="submit"]:hover { background: #7cb342; }
    .cancel-link { margin-left: 20px; color: #aed581; text-decoration: none; }
  </style>
</head>
<body>
  <h2>虫の情報を編集：<%= data.name %></h2>
  
  <form action="/insects/update/<%= id %>" method="POST">
    <table>
      <tr>
        <th>名前</th>
        <td><input type="text" name="name" value="<%= data.name %>" required></td>
      </tr>
      <tr>
        <th>分類</th>
        <td><input type="text" name="type" value="<%= data.type %>"></td>
      </tr>
      <tr>
        <th>発見場所</th>
        <td><input type="text" name="place" value="<%= data.place %>"></td>
      </tr>
      <tr>
        <th>備考</th>
        <td><input type="text" name="note" value="<%= data.note %>"></td>
      </tr>
    </table>
    
    <div class="btn-group">
      <input type="submit" value="修正を保存する">
      <a href="/insects/<%= id %>" class="cancel-link">キャンセルして戻る</a>
    </div>
  </form>
</body>
</html>
