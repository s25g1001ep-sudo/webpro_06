<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>太陽系天体システム</title>
  <style>
    /* 宇宙をイメージしたダークテーマのCSS */
    body { background-color: #0b0d17; color: #e0e0e0; font-family: sans-serif; padding: 20px; }
    h2 { color: #4fc3f7; border-bottom: 2px solid #1a237e; }
    table { width: 100%; border-collapse: collapse; background: rgba(255, 255, 255, 0.05); }
    th, td { border-bottom: 1px solid #333; padding: 12px; text-align: left; }
    th { background-color: #1a237e; color: white; }
    tr:hover { background-color: rgba(255, 255, 255, 0.1); }
    a { color: #4fc3f7; text-decoration: none; }
    .btn { display: inline-block; margin-top: 20px; padding: 10px 20px; background-color: #2e7d32; color: white; border-radius: 5px; }
  </style>
</head>
<body>
  <h2>太陽系天体一覧</h2>
  <table>
    <tr><th>ID</th><th>天体名</th><th>区分</th><th>直径(km)</th></tr>
    <% let number = 0 %>
    <% for( let row of data ) { %>
      <tr>
        <td><%= row.id %></td>
        <td><a href="/solar/<%= number %>"><%= row.name %></a></td>
        <td><%= row.type %></td>
        <td><%= row.diameter %></td>
      </tr>
      <% number += 1 %>
    <% } %>
  </table>
  <a href="/solar/create" class="btn">新しい天体を追加</a>
</body>
</html>